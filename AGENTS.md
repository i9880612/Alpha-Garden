# AGENTS.md

本项目用于自动化开发协作。

核心目标是在保持有效功能和数据安全的前提下，持续降低复杂度，让系统能够长期维护和演进。

任何设计、代码或结构变化，必须至少满足一项：修复真实问题、减少复杂度、提升可验证性、改善核心流程质量或完成明确的阶段交付。不能证明收益的复杂度，不应进入系统。

---

# 一、沟通语言

与用户沟通默认使用中文，采用通俗易懂、明确、可核对的表达，不在中文句子中随意夹杂英文单词。

代码、文件名、命令、配置键、接口字段和原始报错保持原样。必要专有名词出现时，应补充中文解释。

---

# 二、任务边界

开始修改前应明确问题、范围和验证方式。简单、低风险任务可以用一句话说明；跨模块、高风险或可能产生外部副作用的任务再完整说明：

* 当前要解决的具体问题；
* 验收标准；
* 涉及文件及负责人；
* 不触碰范围；
* 影响的核心流程；
* 验证方式；
* 是否计划创建 Git 提交。

任务可以分阶段推进，但每个阶段必须形成可检查成果。准备工作、局部调整、样例改动和表面整理不能单独视为完整交付。

调查获得足以支持结论的证据后必须停止扩张。新发现的问题若不影响当前任务验收，应记录为后续事项，不得自动扩大本轮范围。

## 自动化主流程目标

本项目的业务目标是形成中途不依赖人工干预的自动闭环。人工在运行开始前一次性确定设置、额度、允许的外部操作、停止条件和故障边界；运行开始后，系统不得依赖人工逐条挑选公式、指定变异方向、确认下一批或决定是否继续。

* `generation` 只执行合法生成和变异，不自行声称能够改善公式；
* `learning` 根据可信真实结果决定探索、利用和变异方向；
* `selection` 执行学习策略和预算分配，不把稳定身份排序冒充质量排序；
* `execution` 负责按有界运行计划持续推进、恢复和停止，不拥有公式质量算法；
* 冷启动或证据不足时必须采用明确的探索策略，不得用人工直觉、隐藏规则或未经验证的固定权重冒充学习结果；
* 普通开发预览和人工实验可以用于验证局部能力，但不得成为正式自动主流程中的人工关卡；

重构应优先继承原项目已经证明有价值的自动闭环、分批执行、学习反馈、恢复和有界停止理念。原实现若职责混杂、状态重复或治理过重，应保留业务意图并用更小的实现替代，不为兼容旧结构而恢复旧复杂度。

---

# 三、安全与数据边界

保护真实数据、用户资产和不可逆行为的约束不得因重构、测试便利或提高通过率而削弱。

* 真实外部结果与本地模拟结果必须明确区分，模拟结果不得冒充真实证据。
* 未知、缺失、超时和请求错误不得伪造成成功或确定失败。
* 单条数据或单个任务失败不得无依据地污染其他独立结果。
* 只读操作不得触发写入、提交、发布、删除或其他外部副作用。
* 数据库迁移、模型激活、发布、正式提交和其他不可逆操作必须具有明确目标、失败关闭和可恢复方案。
* 普通开发和测试不得自动触发真实外部操作；需要真实操作时必须确认授权、目标和影响范围。
* 不得通过降低校验标准、跳过检查或伪造状态掩盖失败。

---

# 四、目标架构与依赖方向

代码按照稳定业务能力平铺，不使用 `domain`、`application`、`infrastructure` 等类型分层目录。

```text
execution
    ↓
generation / selection / evaluation / learning / submission
    ↓
persistence / worldquant
```

* `execution` 只负责核心流程顺序、失败边界和恢复，不复制其他模块的算法。
* `generation`、`selection`、`evaluation`、`learning`、`submission` 分别拥有对应业务决策。
* `persistence` 只存取事实和维护事务，不做业务决策。
* `worldquant` 只处理 WorldQuant BRAIN 平台的认证、限流、请求和响应，不决定业务结果。
* 接口和命令行只负责输入、调用和展示，在出现明确需求时再创建。
* 跨模块共享数据使用含义明确的小型数据对象，不传递无边界的巨型字典或上下文对象。
* 不得通过全局工具模块、循环导入、运行时反向注册或新的协调层绕过依赖方向。
* 如果两个模块执行同一业务决策，必须明确唯一所有者并删除重复实现。

---

# 五、状态所有权

每类业务状态必须有唯一权威来源。

* 数据库、文件工件、缓存和内存不得各自维护可独立修改的同一业务事实。
* 派生状态必须能够追溯来源和生成身份；能够以合理成本重算时才要求支持重算，不得反向覆盖权威事实。
* 状态写入点必须明确，读取方不得顺便修正或补写业务状态。
* 跨模块状态变化由应用用例组织，不能依赖隐藏的导入副作用或全局单例。
* 同一事实出现多个来源时，必须先裁决唯一来源，再删除或降级其他副本。

---

# 六、最小保护原则

项目不建立单独的合同体系，不要求为内部实现编写或维护合同，也不通过测试锁死私有函数、调用顺序、文件布局、辅助字段、包装层或模块数量。

只保护三类事项：

* 数据安全和数据正确性；
* 发布、提交、迁移、删除等不可逆操作；
* 用户能够直接观察到且已经确认需要保留的行为。

除上述事项外，内部实现可以随任务自由调整。现有测试若只是在固定内部结构，且不能指出真实失败风险，应当删除或改成直接验证结果。

局部修改引发大范围连锁变化时，不得机械维护所有旧约束。应先删除没有真实保护价值的测试、接口和中间层，再完成当前修改。

---

# 七、复杂度与文件治理

拆分依据是职责、变化原因、状态所有权和依赖边界，不是行数本身。

出现以下情况时必须评估拆分：

* 一个文件包含两个以上可以独立变化的职责；
* 一个服务同时承担流程编排、业务计算和外部读写；
* 一个局部需求需要修改大量无关文件；
* 测试必须构造大量无关状态才能覆盖一个局部行为；
* 同一事实或决策在多个模块重复维护。

文件、类和函数的行数不设置硬门槛。只根据职责数量、依赖关系、修改传播范围和理解成本判断是否拆分，不得为了满足数字机械切割。

拆分必须减少依赖、缩小修改范围或形成清晰所有权。禁止：

* 只把函数搬到新文件，再由原文件逐层转发；
* 建立没有明确业务含义的 `manager`、`helper`、`utils` 或 `common` 杂物模块；
* 为单一实现建立没有替换需要的接口、工厂或适配器；
* 包装层叠加包装层；
* 为未来可能存在的需求预留扩展系统。

新功能不得继续堆入已经承担多项职责的超大文件，除非它确实属于该文件的唯一职责，并且拆分会增加更多依赖。

---

# 八、编码与重构纪律

实现遵循最小必要原则：

* 不做未要求的功能；
* 不引入推测性抽象；
* 不主动兼容未知格式；
* 不增加没有真实证据的兜底分支；
* 不用硬编码、放宽校验、跳过异常或调整阈值掩盖根因；
* 不把算法实验混入保持行为不变的架构重构。

普通修复默认遵循：确认问题 → 定位根因 → 最小修复 → 聚焦验证。

架构重构必须先建立行为基线，再分阶段迁移。每个阶段必须：

* 保持明确列出的有效业务行为；
* 删除被替代的旧代码和重复事实来源；
* 不建立第二条主流程；
* 让修改范围、依赖数量或认知负担至少有一项可证明下降；
* 删除当前替换范围内已经失去作用的旧内容；清理范围明显扩大时，停止扩张并建立独立清理任务。

---

# 九、新旧实现与实验隔离

同一职责不得长期保留两套可执行实现。替换完成时，应删除当前替换范围内的旧入口、配置、开关、兼容层、测试夹具和失效文档；若清理会显著扩大任务范围，应明确拆出后续清理阶段，不得悄悄保留。

只有真实外部数据或线上协议暂时无法直接迁移时，才允许限时兼容。临时兼容必须得到用户明确批准，并记录唯一范围、删除条件和截止阶段。

## 当前开发环境禁止承载实验

本节所称实验，是指为了验证未知收益而进行、结论可能被整体放弃的算法、架构、模型、策略或流程探索。普通功能开发、缺陷修复、代码调试和已经确认方向的重构不属于实验。

实验不得直接在当前开发环境中实现，必须使用从明确代码身份或已记录工作树快照创建的临时独立环境，例如临时 Git 工作树或临时项目副本。

项目始终只保留两个可运行环境：一个日常环境和一个独立实验环境。多个实验或对照版本在同一个实验目录内按已固定的 Git 身份顺序切换，不额外创建实验工作树或项目副本。结束的实验源码、必要输入与结论保留在 Git 历史和历史实验记录中，不以保留多套可运行目录代替归档；未经用户确认有效，不合并到日常环境。

临时实验环境必须与当前开发环境隔离：

* 使用独立目录，不与当前开发环境共同写入代码文件；
* 使用独立配置、数据库、缓存、模型工件、日志和输出目录；
* 不复制或提交密钥、令牌及其他敏感信息；确需访问外部服务时，只通过受控的本地环境注入；
* 默认禁止生产写入、正式提交、发布、迁移和其他不可逆操作；
* 明确记录实验来源的提交、工作树状态、输入、成功条件、失败条件和停止条件；
* 实验结果不得自动影响当前开发环境或生产环境。

## 实验结束后的唯一处理方式

实验完成后只允许以下两种结果：

1. **确认接入**：先由开发者确认实验有效，再从临时环境中提炼最小必要实现，按照正常开发任务重新接入当前开发环境并完成验证。不得直接用整个实验目录覆盖当前开发环境，也不得无审查地合并实验产生的配置、数据和临时工具。
2. **确认放弃**：保持当前开发环境不变，在开发者确认不再需要保留证据后删除临时实验环境及其独立状态。

实验结论尚未裁决时，可以短期保留临时环境，但必须标明负责人、保留原因和待裁决事项，不得成为长期并行开发环境。

删除临时实验环境前必须确认准确路径、必要证据已经归档且没有未提炼的有效成果。删除只针对已确认的临时目录，不得使用仓库根目录、用户目录或来源不明的计算路径作为递归删除目标。

实验被真实证据否定、停止或失去价值后，不得把实验入口、配置开关、兼容分支、状态、测试或文档残留带入当前开发环境，也不以“可能以后有用”为理由保留。

回退优先依赖 Git 或生产路径之外、由开发者明确要求保留的离线工件，不得通过长期保留第二套生产实现提供回退。

不得自动创建数据库备份、运行备份或其他状态快照。只有开发者明确要求时才允许创建，并必须说明保留对象和用途；没有明确要求时，备份目录应保持为空。

---

# 十、错误与事务边界

错误至少区分以下性质：

* 业务拒绝：输入有效，但不满足业务条件；
* 待定状态：外部结果尚未明确；
* 外部故障：网络、平台、存储或依赖暂时不可用；
* 程序缺陷：代码违反自身假设或数据不变量。

不得使用一个通用兜底把不同性质的错误都转换成空结果、失败或重试。

`execution` 决定业务事务范围，`persistence` 提供事务能力。不得让多个数据操作各自提交一个本应原子完成的业务动作，也不得让读取操作隐藏写入副作用。

---

# 十一、测试与验证

所有任务必须形成：输入 → 行为 → 输出 → 验证。

## 测试目录与生产目录对应

`tests` 直接镜像 `src` 的目录结构，不再增加 `unit`、`integration`、`e2e` 等测试类型中间层。生产文件存在聚焦测试时，测试文件使用直接对应的路径和名称。

例如：

```text
src/domain/generation/service.py
tests/domain/generation/test_service.py

src/application/execution.py
tests/application/test_execution.py
```

具体规则：

* 一个生产文件的聚焦测试集中放在对应测试文件中，但不要求每个生产文件都机械创建测试文件；
* 生产文件移动、重命名、拆分或删除时，对应测试目录和测试文件必须在同一阶段同步调整；
* 不得按临时任务名、缺陷编号、开发阶段或实现人员创建测试目录；
* 不得把多个无关生产模块的单元测试长期堆入同一个超大测试文件；
* 测试辅助代码放在距离消费者最近且职责明确的位置，不建立无边界的公共测试工具箱；
* 跨多个生产模块的完整流程验证放在与应用入口对应的测试路径中，不另建测试类型目录；
* 完整流程测试按稳定业务用例命名，不复制局部测试已经覆盖的内部细节。

验证按风险选择，不把全量测试、真实运行或重复审查作为固定门槛：

* 普通修复运行直接覆盖风险点的聚焦测试；
* 跨模块重构先保护稳定业务行为，再验证新的依赖边界；
* 核心流程和数据正确性修改必须有针对性验证；
* 只有局部验证无法证明影响范围时才运行更大测试集；
* 只有本地无法证明关键外部行为时才安排真实环境验证。

测试优先保护外部行为、安全边界和数据不变量，不应逐项镜像私有函数，也不得锁死没有业务意义的内部调用顺序和文件结构。

同一代码身份、同一环境和同一命令已有可信结果时应复用，不机械重复。测试失败必须判断是否由当前差异引起。

---

# 十二、可观测性

可观测性遵循最小必要原则，只保留能够直接支持以下用途的日志或报告：

* 判断核心流程是否成功完成；
* 定位当前可以实际处理的故障；
* 确认不可逆操作的目标和结果；
* 展示用户明确需要查看的业务结果。

新增日志、报告、指标、快照或诊断文件前，应能说明它支持什么运行判断或故障处理。无法说明实际用途时不得新增，不要求为此建立额外审批或说明文档。

严格禁止为了“完整留痕”而记录一切，包括：

* 为每个函数、步骤或状态变化机械记录开始和结束；
* 重复记录数据库或已有业务对象中已经存在的完整数据；
* 为一次性调试长期保留专用报告、证据包或诊断入口；
* 在日志、报告、工件和数据库中重复保存同一份完整结果；日志可以保留必要的身份、状态摘要和故障定位字段；
* 因为将来可能排查而保存没有明确用途的中间状态；
* 让日志、报告或诊断流程反过来改变业务行为。

正常成功路径保持安静，只记录必要结果。可预期的失败使用简洁、可行动的信息；程序缺陷保留足以定位根因的上下文，不复制全部运行状态。

临时调试日志必须在问题关闭的同一阶段删除。失去读取者或处置动作的长期日志、报告、指标和诊断入口应当清理。

日志和报告不得形成第二套业务状态，也不得包含密钥、令牌、个人数据或其他敏感信息。

---

# 十三、协作与文件边界

同一文件不得被多个执行者同时修改。任何执行者不得覆盖来源不明或不属于当前任务的工作区改动。

默认由一个负责人完成实现和必要验证。协作规模按任务风险决定：

* 文档、普通功能、明确修复和只读检查不要求额外角色；
* 只有涉及真实数据、不可逆操作、高风险核心流程或用户明确要求时，才安排独立验证；
* 架构分析和独立审查只在风险或不确定性确实需要时启用，不因跨模块本身自动增加角色；
* 不得为了形式完整创建重复调查、平行实现或重复测试角色。

每个任务只有一个主要实现负责人。没有明确独立验证需要时，同一负责人可以修改生产文件和对应测试并完成验证，不必为文件类型拆分角色。

审查只阻断有证据的严重问题。阻断意见必须给出可复现失败路径、当前差异的触发关系，以及简单修复不足的原因。风格偏好、未来假设和边界外建议只能作为非阻断观察。

## 子智能体创建、职责与复用

子智能体是按需使用的长期职责席位，不是每个临时任务都要重新创建的一次性执行者。

* 默认不创建子智能体。主要负责人能够在合理时间内独立完成并验证的任务，由主要负责人直接完成；不能仅以“可以并行”“想提高速度”或“多一份意见”为理由创建子智能体。
* 下发工作前，必须先检查当前任务树中已经存在的子智能体及其职责。已有子智能体能够承担时，必须续派给原子智能体，不得为同一职责创建新的身份。
* 每个子智能体只保有一个稳定、清楚、互不重叠的职责，例如实现、独立验证或高风险审查。后续同类工作持续复用该身份，不按文件、缺陷、阶段、测试命令或临时子任务创建新身份。
* 只有现有子智能体都不适合，并且任务确实需要独立并行工作、专业能力或风险隔离时，才允许创建新的子智能体。创建前必须说明新职责、不能复用现有身份的原因和完成条件。
* 同一主任务默认最多维护三个子智能体身份；需要超过时，必须先取得用户明确同意。不得通过子智能体继续创建下级子智能体来规避数量限制。
* 职责完成后保留原身份供后续续派，不因代理暂时空闲或已经交付过一次结果就创建替代者。只有原身份不可访问、职责已经永久取消或继续复用会造成明确上下文错误时，才允许替换，并记录原因。
* 复用范围以运行环境实际可访问的任务树为准。跨聊天窗口无法访问旧身份时，应如实说明限制，再按最少数量重建职责；不得声称已经复用不可访问的子智能体。
* 使用子智能体时，阶段交付按职责列出身份、承担事项和结果，让用户能够按职责查找历史；不得只记录含义不明的临时代理名称。

## 子智能体模型与推理强度

创建子智能体时，不固定继承主智能体配置，也不一律使用最高配置。主要负责人根据任务难度、出错代价和结果可验证性选择当时可用的模型与推理强度；三者发生冲突时，优先考虑出错代价和结果可验证性。

* 边界清晰、低风险且结果可机械验证的检索、统计、整理和指定测试任务，可以使用偏重效率的模型以及 `low` 或 `medium` 推理强度；
* 常规缺陷定位、聚焦实现、测试补充和文档核对，使用能力与成本均衡的模型以及 `medium` 或 `high` 推理强度；
* 架构决策、并发与事务、数据库迁移、真实外部操作、数据安全、不可逆行为和独立高风险审查，使用当时可用的高能力模型以及 `high` 或 `xhigh` 推理强度；
* `max` 只用于最困难、质量优先且具有明确验收标准的任务，不得因为任务重要或希望结果更好而默认启用；
* 任务看似简单但出错代价高时不得降级；低配置子智能体的结论不得单独作为高风险决策或不可逆操作的依据；
* 无法可靠判断任务等级时，默认继承主要负责人的模型和推理强度，不得静默降低配置。

具体模型名称和版本属于运行时选择，不固化为长期项目规则。使用子智能体时，阶段交付应列明其任务、实际模型和推理强度；若运行环境只提供继承关系而不提供精确配置，应如实说明继承关系，不得猜测。

---

# 十四、检索、命名与依赖

分析符号定义、调用关系和影响范围时，优先使用项目已有的代码图谱或静态分析能力；搜索字面文本、注释、日志和配置值时使用普通文本搜索。工具不可用时可以使用可靠替代方案，并说明限制。

命名表达稳定职责，不表达阶段、版本或过渡状态。新代码不得使用 `v1`、`v2`、`new`、`legacy`、`improved`、`step1`、`phase2` 等名称区分新旧实现。

不得引入新依赖，除非现有能力无法合理完成任务，并已说明收益、维护成本和替代方案，取得用户批准。

修改前应阅读足以理解边界的相关代码和测试，不以固定文件数量代替理解。

---

# 十五、文档与历史记录

长期规则、当前状态、目标架构和历史实验必须分别维护，不能混在同一文档中。

* `AGENTS.md` 只保存长期开发、安全、架构和协作约束；
* 架构文档保存当前架构事实或明确标记的目标架构；
* 当前状态文档保存工作树、阶段任务、已知问题和下一步；
* 历史实验记录保存假设、输入、结果、结论和退出原因。

阶段性模型口径、运行编号、临时配置和当前默认策略不得固化在 `AGENTS.md`。文档必须明确区分已提交事实、未提交实现和尚未实现的目标。

无效实验的结论应被保留用于避免重复试错，但详细过程不得继续污染生产架构和长期目标文档。

涉及代码的阶段统一按照“开发 → 测试 → 审查 → 文档 → 最终检查 → Git 提交与推送”推进。实现、测试和审查尚未通过时，不得随着每次局部修改反复更新状态、交接、架构或测试数量；审查发现代码问题时应先返回开发环节，完成必要返工并重新验证，全部通过后再于准备提交前一次性补齐文档。文档本身就是任务目标时不受此顺序限制。

---

# 十六、提交与交付

Git 提交以一个可验收阶段或一个具体问题闭环为单位。阶段完成前不得为了保存进度创建正式提交，也不得把多个已完成阶段长期堆积成一次不可审查的大提交。

没有用户明确授权，不得创建提交或推送。Git 提交权限与任何外部系统的发布或正式提交权限彼此独立。

阶段交付至少说明：

* 解决的问题和原因；
* 修改的关键文件和行为；
* 未触碰范围；
* 验证方式和结果；
* 已知风险与后续事项；
* 当前阶段是否达到提交条件；
* 下一步计划。

每次任务完成后的最终回复必须单独列出“下一步计划”。计划应说明下一项唯一优先事项、开始前提和验收结果；如果当前没有合理的后续工作，应明确写明“暂无必要后续，等待用户安排”，不得为了填充计划虚构任务或扩大当前范围。

不强制使用主观总体进度百分比。若任务已有可信里程碑和进度口径，可以更新；否则使用“未开始、进行中、已验证、已交付”等可核对状态。

最终原则：每次修改都应让核心流程更清楚、更安全或更容易改变。
