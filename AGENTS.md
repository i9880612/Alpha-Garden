# AGENTS.md

Alpha Garden 用于 WorldQuant BRAIN 公式研究。目标是在保护真实数据和已确认有效行为的前提下，完成无人值守的研究闭环，并保持实现简单、可验证、易维护。

本文件保留项目特有的业务边界和开发惯例。具体方案、阅读范围和验证方法按当前任务决定。

## 工作方式与完成条件

- 默认用中文沟通，表达具体、可核对；代码、命令、配置键和原始报错保留原文。
- 开始时简述要解决的问题和验收方式。复杂任务再补充关键边界、风险和阶段，不要求每次填写固定清单。
- 在已授权范围内连续完成实现、必要验证和缺陷修复。不要把首次实现、样例或等待常规审查当作任务完成；需要运行或查看界面才能验收时，把这些工作一起完成。
- 常规、可逆的实现选择自行判断，并说明会影响结果的假设。只有缺少影响正确性的关键信息，或下一步超出授权范围时才询问；等待期间继续不依赖该答案的工作。
- 保持修改与当前目标相关。证据足够后停止无关调查，不顺带重构、扩展功能或创建新任务。
- 保留不属于本任务的工作区改动，不覆盖来源不明的修改。

## 无人值守的业务要求

运行开始前由用户确定设置、预算、允许的外部操作、停止条件和故障边界。开始后，系统应按这份计划自行推进、恢复和停止，不依赖人工逐条挑公式、指定变异方向或确认下一批。

- 研究决策使用可信的真实结果；冷启动或证据不足时采用明确的探索策略，不把固定排序或未经验证的权重称为学习结果。
- 生成合法候选不等于改善质量；是否继续投入由真实反馈和预算决定。保留已经验证有效的分批执行、学习反馈和有界停止行为。
- 单条数据或任务失败应限制在其影响范围内，其他独立工作继续。重试、等待和名额占用必须有边界，不能因一个对象长期卡住整个运行。
- 账号级故障、结果不确定和预算耗尽按各自边界处理。不要为了持续运行而绕过校验、无限重试或伪造成功。
- 开发预览和人工实验不构成正式自动流程中的人工关卡。

## 数据与授权边界

- 区分真实结果、模拟结果和界面示例。未知、缺失、超时与请求错误不得改写为成功或确定失败；不得降低校验标准来掩盖问题。
- 只读操作不附带业务写入、补数据、提交、发布或删除。普通开发、构建和使用隔离测试数据的验证，不得自动访问真实账号或触发平台操作。
- 真实平台操作、数据库迁移、模型激活、正式提交、发布及不可逆删除，应有明确授权、目标、影响范围和恢复办法；无法确认前置条件时不执行。已有授权覆盖的步骤不重复请示。
- Git 提交或推送需要用户明确授权，与平台公式提交或发布授权分别判断。修改完成不代表自动获得提交或推送权限。
- 密钥、令牌和敏感个人数据不得写入日志、报告或仓库。真实公式表达式、本地数据库和运行日志等研究资产不进入公开仓库，对外披露需明确授权。保留现有敏感文件忽略规则。
- 不自动创建数据库备份、运行备份或状态快照；仅在用户明确要求时创建，并说明对象和用途。已有备份不得因本条规则自动删除。
- 优先使用现有依赖；确需新增依赖时，先说明必要性、维护成本和替代方案并取得批准。用户已明确授权的依赖恢复或引入不再重复询问。

## 架构与状态所有权

Python 代码按稳定业务能力平铺于 `src/`，不引入 `domain`、`application`、`infrastructure` 类型分层。

```text
execution
    ↓
generation / selection / evaluation / learning / submission
    ↓
persistence / worldquant
```

| 模块 | 所有权 |
| --- | --- |
| `execution` | 流程顺序、运行计划、失败边界、恢复和业务事务范围 |
| `generation` | 合法生成和变异 |
| `learning` | 根据真实结果决定研究方向 |
| `selection` | 执行学习策略和预算分配 |
| `evaluation` / `submission` | 各自的评价与提交业务决策 |
| `persistence` | 事实存取和事务能力 |
| `worldquant` | 平台认证、限流、请求与响应处理 |

命令行、接口和前端负责输入、调用与展示，复用现有业务决策，不另建一套研究主流程。跨模块数据用含义明确的小型对象，不通过巨型上下文、循环导入或隐藏的全局注册绕过依赖方向。

每类业务事实只有一个权威来源和明确写入点。缓存、报告和派生状态应可追溯来源，不独立修改同一事实或反向覆盖它。读取方不顺便修正状态；应原子完成的业务动作放在同一事务中，不由各存储操作分别提交。

## 实现与错误处理

- 优先解决根因，用当前需求所需的最小实现完成目标。避免推测性抽象、无依据的兜底、未知格式兼容和只转发调用的包装层。
- 按职责、变化原因和状态所有权决定拆分。拆分应缩小修改影响或降低理解成本，不按行数、文件数或预设层数机械组织代码。
- 一个业务决策只有一个所有者。替换时清除当前范围内被替代的入口、重复状态、失效配置和测试；不长期保留两套可执行实现。清理明显超出范围时说明剩余项。
- 临时兼容仅用于确实无法直接迁移的真实数据或线上协议，经用户批准后明确范围、退出条件和截止阶段。
- 命名反映稳定职责，不用 `new`、`legacy`、`v2` 等过渡称呼代替职责。避免无边界的 `manager`、`helper`、`utils` 杂物模块。
- 区分业务拒绝、结果待定、外部故障和程序缺陷，分别处理；不把它们统一吞成空结果、失败或重试。

## 实验隔离

此处的实验指验证未知收益、结论可能被整体放弃的算法、模型、架构或流程探索。普通功能开发、修复、调试和已确认方向的重构不属于实验。

- 实验使用独立目录，并固定来源提交及未提交改动（如有）。项目保留一个日常环境，至多一个独立实验环境；多个实验在该实验环境中顺序切换，不额外复制可运行项目。
- 实验的配置、数据库、缓存、模型工件、日志和输出与日常环境隔离。密钥不复制进实验；必要服务访问通过受控的本地环境注入。默认禁止生产写入、正式提交、发布和迁移。
- 记录必要的来源、输入、成功与失败条件、停止条件和结论；不为留痕复制完整运行状态。
- 只有用户确认实验有效后，才提炼最小实现接回日常环境并验证；不整目录覆盖，也不让实验结果自动改变生产状态。
- 未裁决的实验只短期保留，并说明待裁决事项。放弃的实验不向日常环境遗留入口、开关或状态；必要证据归档并经用户确认不再需要后，核对准确路径再删除实验目录。
- 用 Git 历史和必要结论保留实验经验，不靠长期保留多套运行环境提供回退。

## 验证与交付

验证服务于当前风险和验收结果，不以测试数量、全量检查或固定阶段顺序作为完成标准。

- 数据安全、数据正确性、不可逆操作和用户已确认的可见行为需要直接证据；不为私有函数、调用顺序、辅助字段或内部布局建立合同式测试。
- 普通修复选择覆盖受影响行为的检查；只在局部验证不能证明影响范围时扩大范围。复用同一代码和环境下已有的可信结果，因实际修改或失败再重跑相关检查。
- 使用临时数据、不会访问真实账号的本地测试，以及静态检查和构建，可以连续执行、修复当前修改引起的失败并复验，无需逐步批准。不要把所有测试或真实运行一概视为安全。
- 界面修改应查看实际渲染和相关交互；纯文档修改检查内容、路径、链接与前后规则一致性，无需因此运行应用测试。
- `tests/` 镜像 Python 的 `src/` 业务目录，例如 `src/learning/frontiers.py` 对应 `tests/learning/test_frontiers.py`。生产文件迁移时同步调整相关测试，不新增 `unit`、`integration`、`e2e` 中间目录，也不要求每个文件机械配测试。
- 架构重构先明确需要保留的行为，再按可验收范围迁移。审查中有证据影响正确性、安全或验收的缺陷应修复；风格偏好和范围外设想不阻断交付。
- 行为或使用方式变化时更新相关文档，不随每次局部修改反复生成状态报告。交付说明实际结果、必要验证、剩余限制和提交状态即可；有实际后续时再列下一步，不虚构进度或扩展任务。
- Git 提交以可独立验收的改动为单位，完成必要验证并获得授权后执行。

## 按需协作与上下文

默认由一个负责人实现并验证，不为形式完整安排额外角色。只有需要独立验证高风险行为、隔离职责或用户明确要求时才委派。

委派前检查现有可访问的子智能体，优先复用职责匹配者；职责和文件修改范围保持清楚，不让多人同时修改同一文件。不按临时缺陷反复创建身份，不声称复用了不可访问的跨任务身份。同一主任务默认最多三个子智能体，不通过下级创建绕过上限；超出时先征得用户同意。

模型和推理强度按难度、出错代价和结果可验证性选择，不固定用最高配置，也不让低配置结论单独决定高风险操作。无法判断时继承主智能体配置。发生委派时说明职责、身份和实际配置；配置不可见时如实说明。

只读取本次任务需要的上下文：使用说明查 `README.md`，前端启动与界面预览查 `webui/README.md`，模块行为查对应源码和测试；架构、数据库或部署资料只在涉及其边界时读取。技能也按实际适用场景使用，不因关键词或“必须使用”的宽泛描述加载无关流程。

`AGENTS.md` 只保留仍然必要的长期规则。当前运行编号、临时设置、模型版本和实验细节放在相应状态或历史记录中，不堆进本文件，也不为了分层而新增无人使用的文档。文档区分已实现事实、未提交修改和尚未实现的目标。

日志和报告只保留能支持运行判断、故障处理、不可逆操作确认或用户查看结果的信息，不形成第二套业务状态。临时调试输出随问题关闭清理，不长期保留一次性证据包。

维护本文件时合并重复规则、修正过时示例；新增约束应指出实际保护对象和适用条件，避免把一次失败补成所有任务都要执行的流程。
